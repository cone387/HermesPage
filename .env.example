# HermesPage 环境变量配置

# JWT 签名密钥（生产环境必须设置固定值，否则重启后旧 token 失效）
HERMES_JWT_SECRET=change-me-to-a-random-string

# 预设管理员账号（可选，不设则首次启动进入 setup 引导页）
# HERMES_ADMIN_USER=admin
# HERMES_ADMIN_PASS=your-password

# 服务端口
# HERMES_PORT=5487
